<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_gradient"
    android:orientation="vertical"
    android:padding="24dp"
    android:gravity="center">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Doğru Cevap Görme Hakkı"
        android:textSize="22sp"
        android:textStyle="bold"
        android:textColor="@color/bb_text"/>

    <TextView
        android:id="@+id/tvAdInfo"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:text="Reklam izleyerek 1 hak kazan"
        android:textSize="15sp"
        android:textColor="@color/bb_text_muted"
        android:gravity="center"/>

    <Button
        android:id="@+id/btnWatchAd"
        style="@style/BBButton"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:layout_marginTop="32dp"
        android:text="@string/btn_watch_ad_unlock"/>

    <Button
        android:id="@+id/btnBack"
        style="@style/BBButtonAlt"
        android:layout_width="wrap_content"
        android:layout_height="48dp"
        android:layout_marginTop="16dp"
        android:text="@string/btn_back"/>
</LinearLayout>
