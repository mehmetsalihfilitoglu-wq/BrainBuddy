<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="32dp"
    android:background="@drawable/bg_gradient">

    <ImageView
        android:id="@+id/emptyMascot"
        android:layout_width="132dp"
        android:layout_height="132dp"
        android:src="@drawable/ic_edumio_mascot_sleeping"
        android:contentDescription="@string/dc_cd_mascot"
        android:layout_marginBottom="16dp"/>

    <TextView
        android:id="@+id/emptyTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/dc_review_empty"
        android:textSize="17sp"
        android:textStyle="bold"
        android:textColor="@color/edu_text_dark"
        android:gravity="center"/>

    <TextView
        android:id="@+id/emptySubtitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:text="@string/dc_review_empty_sub"
        android:textSize="14sp"
        android:textColor="@color/edu_text_muted"
        android:gravity="center"/>

    <Button
        android:id="@+id/emptyClose"
        style="@style/Edu.Button"
        android:layout_width="wrap_content"
        android:layout_height="52dp"
        android:layout_marginTop="24dp"
        android:paddingStart="28dp"
        android:paddingEnd="28dp"
        android:text="@string/dc_result_home_cta"/>

</LinearLayout>
